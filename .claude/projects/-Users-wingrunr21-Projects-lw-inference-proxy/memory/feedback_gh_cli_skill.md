---
name: Use gh-cli skill for GitHub operations
description: User wants the gh-cli skill invoked for any GitHub-related work (accessing repos, PRs, releases, actions)
type: feedback
---

Always invoke the `gh-cli` skill before doing GitHub-related tasks (releases, PRs, issues, Actions workflows, repo operations).

**Why:** User previously requested this and expects it as standard behavior in this project.

**How to apply:** When writing or reviewing GitHub Actions workflows, creating releases, working with PRs/issues, or any other GitHub operation — invoke the `gh-cli` skill first.
